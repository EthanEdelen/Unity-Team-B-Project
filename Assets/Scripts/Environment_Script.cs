using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Environment_Script : MonoBehaviour
{
    private Vector3 InitalSpawn;
    private Vector3 WaveLocation;
    private Vector3 DespawnSpot;
    bool reachedWaveLocation;
    bool reachedDespawnLocation;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    void InfoHandoff()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
